#include <stdio.h>
#include<stdlib.h>
#include<math.h>

int b_rand(int x);
int main()
{
	srand((unsigned)time(NULL));

	int i, j;
	char n;

	do
	{
		if=
	}
}

int b_rand(int x)
{
	x = rand() % 2;

	return x;
}